# Batch analytics component

Tenantbatchapp.py is the implementation of batch analytics component. The application takes as arguments the silver data locations, aggregates the results and stores produced gold data into HDFS gold data storage.